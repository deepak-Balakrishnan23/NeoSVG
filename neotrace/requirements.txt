# NeoSVG dependencies
# Install: pip install -r requirements.txt

# ── Core image processing (required) ──────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Sub-pixel marching-squares contour extraction (required by the NeoSVG Engine)
scikit-image>=0.21.0

# ── SVG rasterisation for the quality validator ───────────────────────────
# cairosvg needs the native Cairo library. The pipeline degrades gracefully
# (SSIM is reported as n/a) if Cairo is unavailable.
cairosvg>=2.7.0

# ── CLI / reporting (required) ────────────────────────────────────────────
click>=8.1.0
rich>=13.0.0

# ── Web server (required for server.py) ───────────────────────────────────
Flask>=3.0.0

# ── OCR text preservation (optional — pipeline degrades gracefully) ───────
paddlepaddle>=2.5.0
paddleocr>=2.7.0

# ── Foreground/background segmentation (optional — degrades gracefully) ───
rembg>=2.0.50
